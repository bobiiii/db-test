const {
  addBlogController, blog, getBlogs, updateBlogController, deleteBlog,
} = require('./blogConrollers');

module.exports = {
  addBlogController,
  blog,
  getBlogs,
  updateBlogController,
  deleteBlog,
};
