const {
  getAllGalleries, getGallery, createGallery, updateGallery, deleteGallery,
} = require('./galleryControllers');

module.exports = {
  getAllGalleries, getGallery, createGallery, updateGallery, deleteGallery,
};
